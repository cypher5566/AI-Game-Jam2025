# FastAPI 核心
fastapi>=0.110.0  # 升級以支援 anyio 4.x（google-genai 需要）
uvicorn[standard]>=0.27.0  # 升級以支援 anyio 4.x
python-multipart==0.0.6

# 圖片處理
pillow>=10.1.0

# AI 服務
# google-generativeai==0.3.1  # DEPRECATED - 將於 2025/8/31 停止支援
google-genai>=1.0.0  # 新的統一 Google GenAI SDK (支援 Gemini 2.5 Flash Image)

# 資料庫
supabase>=2.0.0  # 使用最新版本以支援 httpx 0.25.x

# Google Sheets 整合
gspread==5.12.0
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# WebSocket
websockets>=13.0  # 升級以支援 google-genai

# 資料驗證
pydantic==2.5.0
pydantic-settings==2.1.0

# 環境變數
python-dotenv==1.0.0

# HTTP 客戶端
httpx>=0.24.0  # 與 supabase 兼容的版本

# 工具
python-jose[cryptography]==3.3.0  # JWT 處理
passlib[bcrypt]==1.7.4  # 密碼處理
